---
title: "Thomas Keggin tries to figure out R Markdown"
output: html_notebook
---

Hello, I am going to load in some nice packages.

```{R, message = FALSE}
library(tidyverse)
library(readxl)
library(knitr)
library(DT)
```

Set the working directory:

```{r, setup}
knitr::opts_knit$set(root.dir = 'C:/Users/thoma/OneDrive/Documents/PhD/data/megafauna')
```

Load in some data and display it:

```{r}
data <- read_excel("./shortlist.xlsx")
kable(data)
```

